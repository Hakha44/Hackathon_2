{% extends 'base.html.twig' %}

{% block title %}Nouveau Service{% endblock %}

{% block body %}
    <h1>Créer un Service</h1>

    <div class="row">
        <div class="col text-right">
            <a class="btn button" href=href="{{ path('service_index') }}">Retour</a>
        </div>
    </div>

    {{ include('service/_form.html.twig') }}

{% endblock %}
