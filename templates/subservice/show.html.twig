{% extends 'base.html.twig' %}

{% block title %}Subservice{% endblock %}

{% block body %}
    <h1>Subservice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subservice.id }}</td>
            </tr>
            <tr>
                <th>Sous-service</th>
                <td>{{ subservice.name }}</td>
            </tr>
            <tr>
                <th>Service</th>
                <td>{{ subservice.service.name }}</td>
            </tr>

        </tbody>
    </table>

    <div class="centerer">

        <a href="{{ path('subservice_index') }}" class="button">back to list</a>

        <a href="{{ path('subservice_edit', {'id': subservice.id}) }}" class="button">edit</a>

    </div>

    {{ include('subservice/_delete_form.html.twig') }}
{% endblock %}
