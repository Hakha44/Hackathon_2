{% extends 'base.html.twig' %}

{% block title %}Subservice{% endblock %}

{% block body %}
    <h1>Subservice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subservice.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ subservice.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('subservice_index') }}">back to list</a>

    <a href="{{ path('subservice_edit', {'id': subservice.id}) }}">edit</a>

    {{ include('subservice/_delete_form.html.twig') }}
{% endblock %}
