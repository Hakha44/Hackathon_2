<form method="post" action="{{ path('subservice_delete', {'id': subservice.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce sous-service ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subservice.id) }}">
    <button class="btn">Delete</button>
</form>
