{% extends 'base.html.twig' %}

{% block title %}Nouveau sous-service{% endblock %}

{% block body %}
    <h1>Créer un sous-service</h1>

    <div class="row">
        <div class="col text-right">
            <a class="btn button" href="{{ path('subservice_index') }}">Retour</a>
        </div>
    </div>

    {{ include('subservice/_form.html.twig') }}

{% endblock %}
