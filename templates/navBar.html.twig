<header class="header mb-5 container-fluid">
    <nav class="navbar navbar-expand-lg fixed-top">
        <div class="container"><a href="#" class="navbar-brand"> <img class="logo" src="/assets/images/logo.png"
                                                                      alt="Lab'O"></a>
            <button type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                    aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler navbar-toggler-right">
                <span class="navbar-toggler-icon"></span>
                <i class="fa fa-align-justify"></i></button>
            <div id="navbarSupportedContent" class="collapse navbar-collapse">
                <ul class="navbar-nav ml-auto">

                    <li class="nav-item"><a href="{{ path('service_index') }}" class="nav-link">Service</a></li>
                    <li class="nav-item"><a href="{{ path('subservice_index') }}" class="nav-link">Sous service</a></li>
                    <li class="nav-item"><a href="{{ path('event_index') }}" class="nav-link">Evenements</a></li>
                    <li class="nav-item"><a href="{{ path('participant_index') }}" class="nav-link">Participants</a>
                    </li>
                    <li class="nav-item"><a href="{{ path('form_builder_index') }}" class="nav-link">Formulaire</a></li>
                </ul>
            </div>
        </div>
    </nav>
    {% include 'header.html.twig' %}
</header>